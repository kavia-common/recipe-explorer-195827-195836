# API configuration (optional)
# If neither is set, the app uses local mock recipes.
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=

# Feature flags (optional)
# Comma-separated list of flags, e.g. "experiments"
REACT_APP_FEATURE_FLAGS=

# Optional boolean-style flag (true/false/1/0/yes/no/on/off)
REACT_APP_EXPERIMENTS_ENABLED=
